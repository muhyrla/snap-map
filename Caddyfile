{
    email {$ACME_EMAIL}
}

{$DOMAIN} {
    # API запросы → бэкенд
    handle /api/* {
        reverse_proxy backend:8080
    }

    # Всё остальное → фронт
    handle {
        reverse_proxy frontend:80
    }
}
